<!DOCTYPE html>
<html>
<head>
  <title>Demo</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div class="container">
		<ul class="nav nav-pills">
  			<li ><a href="#">Treehouse</a></li>
  			<li><%= link_to "All statuses", statuses_path %></li>
	
			<% if user_signed_in? %>
				<li class="pull-right"><%= link_to "log out" , destroy_user_session_path, method: :delete %>
				<li class="pull-right"><%= link_to current_user.full_name, edit_user_registration_path %></li>
			<% else %>
				<li class="pull-right"><%= link_to "Register", new_user_registration_path %> </li>
				<li class="pull-right"><%= link_to "Log in", new_user_session_path %> </li>
			<% end %>
		</ul>
	</div>
	<div class="container">
	   <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
		<%= yield %>
	</div>
</body>
</html>
